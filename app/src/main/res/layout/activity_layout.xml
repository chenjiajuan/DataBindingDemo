<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
         <variable name="user" type="com.chenjiajuan.databingdemo.data.User"></variable>
        <variable name="presenter" type="com.chenjiajuan.databingdemo.activity.LayoutActivity.ClickPresenter"></variable>
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activity.LayoutActivity"
        android:orientation="vertical"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="center"
            >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="姓名:"
            />
        <EditText
            android:layout_width="100dp"
            android:layout_height="30dp"
            android:hint="@{user.name}"
            android:gravity="center"
            android:onTextChanged="@{presenter.onTextChanged}"
            />
        </LinearLayout>
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            >
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="密码："
                />
            <EditText
            android:layout_width="100dp"
            android:layout_height="30dp"
            android:hint="输入密码"
            android:gravity="center"
                android:textSize="12sp"
            />
        </LinearLayout>
        <Button
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="提交:click"
            android:textSize="13sp"
            android:layout_marginLeft="17dp"
            android:onClick="@{presenter.submit}"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="提交:viewModel::click"
            android:textSize="13sp"
            android:layout_marginLeft="17dp"
            android:onClick="@{presenter::submit3}"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="提交:()->viewModel.click()"
            android:textSize="13sp"
            android:layout_marginLeft="17dp"
            android:onClick="@{()->presenter.submit2()}"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="提交:()->viewModel.click(args)"
            android:textSize="13sp"
            android:layout_marginLeft="17dp"
            android:onClick="@{()->presenter.submit4(user.password)}"
            />
    </LinearLayout>
</layout>
